{
  "name": "cursor-trello-agent",
  "version": "1.1.0",
  "description": "Bridge Trello webhooks to Cursor AI agent — auto-triggers development workflows when cards are labeled",
  "bin": {
    "cursor-trello-agent": "./bin/cli.js"
  },
  "keywords": [
    "cursor",
    "trello",
    "webhook",
    "ai",
    "agent",
    "automation"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "dotenv": "^16.0.0",
    "@ngrok/ngrok": "^1.0.0"
  },
  "optionalDependencies": {
    "@ngrok/ngrok-win32-x64-msvc": "^1.7.0"
  },
  "files": [
    "bin/",
    "src/",
    "templates/",
    "README.md"
  ]
}
